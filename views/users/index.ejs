 <!DOCTYPE html>
 <html lang="en">
 <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products Admin Page</title>
 </head>
 <body>
    <h2>Users Admin Page</h2>
    <a href="/users/add">Add User</a>

    <ol>
    <% users.forEach(user =>{%>
        <li><%= user.name %>-
            <%= user.email %>-
            $<%= user.pass %>
            <a href="/users/<%= product._id %>/edit">Edit</a>
            <a href="/users/<%= product._id %>/delete">Delete</a>
        </li>
   <% }) %>
    </ol>
 </body>
 </html>